using System.IO;
using System;

class Program
{
   static void Main()
    {
       int[] arr = new int[] ;
       int sum = 0;
       Console.WriteLine("eneter the element");
      
       for(int i=0;i<arr.Lengh;i++)
       {
           
       arr[i] = convert.ToInt32(Console.ReadLine())
      }

for (int i = 0; i < arr.Length; i++)
{
    sum += arr[i];
}
Console.WriteLine(sum);
       
    }
}